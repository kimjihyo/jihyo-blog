import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    margin: '2em 0',
  },
  formContainer: {
  },
  titleTextField: {
    marginBottom: '1em',
  },
  createButton: {
    marginTop: '2em',
  },
});

export default useStyles;
