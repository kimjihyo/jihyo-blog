import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {

  },
  breadcrumbs: {
    marginBottom: '1em',
  },
  title: {
    marginBottom: '1em',
  },
});

export default useStyles;
